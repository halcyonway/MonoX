# c98d362 — feat: reasoning 归为基本 output，debug 只加 loop 内部细节

**重构**：

- reasoning 从「debug 专属」改为「基本 output」—— 正常 mode 也展示（用户要看 LLM 怎么想）
- debug mode 仍独有：state 切换 / metric / tool args 等 loop 内部细节
- 删除 `MONOX_DEBUG` env var，仅 `--debug` CLI flag

理由：reasoning 对用户透明是好事（debug / 排错）；debug mode 应聚焦「loop 自己做什么」而非「LLM 想什么」。两层清晰不重叠。

`extensions/channels/terminal.py` / `run.py` / spec/impl-v0.8.md 同步更新。3/3 e2e 回归 pass。