# 69a8b5e — feat(v0.4): Rich-powered terminal UI

终端 channel 用 Rich 重写：

- `Console.print` 替代 print
- `Markdown` 渲染 assistant 输出（流式 update）
- `Panel` 渲染 tool result（cyan border）
- `Spinner` thinking 状态（v0.8.2 才上，更早用纯文本「thinking...」）
- role 颜色：user=cyan / assistant=green / system=grey

依赖加 `rich>=13`。输入仍 `input()`。