# 3a6e425 — feat(v0.3): clean terminal output + move adapter to extensions

**TerminalChannel 移出 core**

- `core/channels/` → `extensions/channels/`
- core 现在零 UI 依赖（仅 `httpx` / `tomli`）
- rule.md「core 固化 / extension 扩展」原则落地

**输出清理**

- 用 rich 之前先 raw text，reactive 风格 print 大量调试
- 加最小 Rich：颜色化用户/assistant 角色提示符
- tool result 单行展示（避免 stdout 全 dump）

terminal 输入仍是 `input()`，下个 commit (v0.7) 才换 prompt_toolkit。