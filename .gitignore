__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.env
.idea/
.vscode/
.DS_Store
*.log

# runtime data directories (never commit)
# .monox/ 包含 memory/ skills/ logs/ checkpoints/ 等运行时数据
.monox/
# 文件日志（按日期 monox-YYYY-MM-DD.log，可能含 LLM 请求/响应内容）
.monox/logs/
workspace/
memory/
tmp/

# skills source code (not runtime data)
# !extensions/skills/  ← REMOVED: skills live at <sandbox.skills_root>/ only
!core/memory/

# local config — keep config.example.toml as template, ignore user's config.toml
config.toml

# AI agent instructions (CLAUDE.md / AGENT.md) are personal per-developer,
# never commit them. Use spec/ for project-shared conventions.
CLAUDE.md
AGENT.md
CLAUDE.local.md
AGENT.local.md