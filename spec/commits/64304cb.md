# 64304cb — spec: record v0.1 implementation decisions and pitfalls

记录 v0.1 实现期决策与踩坑：

- LLM 流式 chunk 区分 `delta.content` vs `delta.reasoning_content`（provider-specific 字段名差异）
- tool_call 解析：流中可能跨 chunk，要按 `tool_call.index` 聚合 args
- checkpoint 恢复点：每轮 final 后写，避免半轮状态
- bash tool cwd 解析：相对路径以 session workspace 为 base
- LLMProxy 不在 core 暴露 SDK 类型（OpenAI/Anthropic）→ httpx raw，避免污染 protocol

记入 `spec/2026-08-10-impl-v0.1.md`（已迁移至 commits/ 目录后被删除）。