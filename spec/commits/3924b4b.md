# 3924b4b — refactor: move main out of core, add install.sh, default .monox/

**main 移出 core**

- 删除 `core/main.py`
- 新建 `run.py`（顶层，不属于 core / extensions）
- run.py 装配：Config.load → MemoryStore → Sandbox → LLMProxy → Gateway → Loop
- 理由：core 不应该知道完整 runtime 怎么启动

**install.sh**

- 一行 bash：`uv sync && cp config.example.toml config.toml`
- 让用户最少步骤跑起来

**默认 ./.monox/**

- 配置 / checkpoint / memory 默认放 `./.monox/`（不是 `~/.monox`）
- 项目本地的 gitignore 友好
- config `[paths]` 可覆盖

`tests/test_e2e.py` 用临时 `.monox/` 跑，不污染用户目录。