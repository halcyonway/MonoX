__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.env
.idea/
.vscode/
.DS_Store
*.log

# runtime data (mounted via docker, not in repo)
workspace/
memory/
tmp/

# core/memory is source code, not runtime data
!core/memory/

# local config — keep config.example.toml as template, ignore user's config.toml
config.toml