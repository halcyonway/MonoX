# a4e817a — feat: implement full MonoX runtime core

实现完整 runtime，4/4 e2e 全绿：

- `core/loop/engine.py`: ReAct 循环（user → LLM stream → tool_call dispatch → tool_result → 再 LLM，直到 stop）
- `core/loop/tool_registry.py`: tool 注册表 + 内置 bash / read_tr_budget / skill_load / wait_io
- `core/loop/checkpoint.py`: jsonl append-only，崩溃恢复
- `core/loop/context.py`: `compress_tool_result` 占位（L1/L2 接口）
- `core/loop/metric.py`: turn/step/latency/tokens 采集
- `core/gateway/dispatcher.py`: in_q pump（channel → loop）+ out_q pump（loop → channel）+ 两端 cleanup
- `core/sandbox/bash_runner.py`: asyncio subprocess，timeout + cwd 控制
- `extensions/channels/terminal.py`: 首个真实 channel（基于 input()，后被 prompt_toolkit 替换）
- `extensions/skills/`: 第一个 skill 占位
- `pyproject.toml`: httpx / tomli 依赖
- `config.example.toml`: 全字段模板

通过 e2e：basic_bash / wait_io_ends_turn / queue_aggregate_continues_react / chat_only_persists_across_restart。